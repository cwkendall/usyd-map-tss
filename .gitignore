node_modules/
dist/
.venv/
__pycache__/
*.pyc
.DS_Store
# Excel lock files
~$*.xlsx
# generated data is reproducible from the workbook; keep it out of git
# (the deploy workflow regenerates it). Comment out to commit snapshots.
public/data/*.geojson
public/data/*.json
# EXCEPT footprints: sourced from OSM (not the workbook), generated by
# `npm run footprints`, committed and used as-is by the app + CI build.
!public/data/facility-footprints.geojson
